using System;
using System.Collections.Generic;
using System.Data;
using System.Data.SqlClient;
using System.Text;

namespace ADONET
{
    public class Execution
    {
        public void Execute()
        {
            //  Console.WriteLine("ADONET Example in .Net Core");
            // new ConnectToSQLServer().SQLQuery();
            // new ConnectToSQLServer().SQLSP();
            //new ConnectToSQLServer().RecordReader();
            //string LoginId = Console.ReadLine();
            //new ConnectToSQLServer().RecordReaderByLoginId(LoginId);
            //string EmpName = Console.ReadLine();
            //new ConnectToSQLServer().FilterReaderByName(EmpName);
            // string LoginId = Console.ReadLine();
            //string result =  new ConnectToSQLServer().GetEmpNameByLoginId(LoginId);
            // Console.WriteLine(result);
            //new ConnectToSQLServer().RecordAdapter();
        }
    }
}
